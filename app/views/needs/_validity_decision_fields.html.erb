<%= f.semantic_fields_for :status do |s| %>
  <%= s.input :reason,
              :label => "Why is this need out of scope?",
              :hint => "Please explain why this need is not in scope for GOV.UK",
              :as => :text,
              :input_html => { :class => 'input-md-8', :rows => 3 } %>
<% end %>
